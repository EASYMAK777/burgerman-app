<h1>Burgers!</h1>

<h2>Cartoon Burgers are the best!!!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      <li>

        {{burger_name}}
        <button data-id= {{id}} class="change-devoured">Devour Burger</button>
      </li>
    {{/unless}}
  {{/each}}
</ul>

<h2>Cartoon burgers are devoured!</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
      <li>
        {{burger_name}}
      </li>
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="name">
  </div>

  <div class="form-group">
    
    <input type="radio" name="devoured" value="1" checked> devoured!<br>
    
  </div>

  <button type="submit">Add Burger</button>
</form>
